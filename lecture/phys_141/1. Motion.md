## 2025-09-26

Use a particle model to simplify

Representations of motion:
- Graphs
- Vectors
- Motion diagrams
	- Positions separated by equal intervals of time
- Mathematical

> **Kinematics**
> A pure description of motion (no causes, just description)

> **Position**
> Where something is at an instant in time
> $$ arrow(r) $$
> I.e. a vector from the origin to the location of the object.

$arrow(i)$, $arrow(j)$, and $arrow(k)$ are unit vectors in the xyz coordinate system.

> **Displacement**
> $$ Delta arrow(r) equiv arrow(r)_2 - arrow(r)_1 $$

> **Time interval**
> $$ Delta t = t_2 - t_1 $$

> **Velocity**
> $$ arrow(v)_"avg" equiv (Delta arrow(r)) / (Delta t) $$

Recall:
$$ arrow(r)_2 = arrow(r)_1 + Delta arrow(r) = arrow(r)_1 + arrow(v)_"avg" Delta t $$

Non-constant velocity is indicated by a non-constant first derivative on the position graph of an object.

> **Instantaneous velocity**
> Let $Delta arrow(x) arrow d arrow(x)$
> $Delta t arrow d t$
> $$ arrow(v) equiv (d arrow(x)) / (d t) $$
> $d t$ must be small enough that $arrow(v)$ looks constant.

## 2025-09-29

$$ Delta arrow(s) equiv arrow(s)_f - arrow(s)_i arrow.double arrow(s)_f = arrow(s)_i + Delta arrow(s) $$

> *Reminder*
> $$ arrow(A) = A hat(i) $$

$$ v_"avg" equiv (Delta s) / (Delta t) = (s_f - s_i) / (t_f - t_i) $$

Slope of $s$ vs. $t$ = velocity
Area under graph of $v$ vs. $t$ = displacement

Average velocity is the constant velocity required to achieve the same displacement in the same time interval.

For a uniformly increasing velocity starting at 0, $v_"avg" = 1/2 v_f$.

For a uniformly increasing velocity (not necessarily starting at 0), $Delta s = 1/2 (v_f - v_i)(t_f - t_i) + v_i (t_f - t_i)$.
Or, $Delta s = 1/2 Delta v Delta t + v_i Delta t$.

$$ arrow(a)_"avg" = (Delta arrow(v)) / (Delta t) = (arrow(v)_f - arrow(v)_i) / (Delta t) $$

> *Demo*
> Metal vs. paper disc falling in a vacuum

Recall $Delta s = 1/2 Delta v Delta t + v_i Delta t$.
- $a_"avg" = (Delta v) / (Delta t) arrow.double Delta v = a_"avg" Delta t$
- $Delta s = v_i Delta t + 1/2 a Delta t^2$
- (Assuming $a$ is constant)

## 2025-10-01

IFF $a$ is constant, then:
$$ v_"avg" = 1/2 (v_i + v_f) $$
$$ s_f = s_i + v_i Delta t + 1/2 a Delta t^2 $$

> **Free fall**
> Near Earth's surface (neglecting air resistance), all objects with mass experience the same constant acceleration.
> $$ arrow(a) = -g hat(j) $$
> where $g = 9.8 m/s^2$

For an **inclined plane**, in the absence of friction and air resistance, $a = g sin theta$ down the ramp.

**Slowing down**: When acceleration is in the *opposite* direction (sign) from velocity
**Speeding up**: When acceleration is in the *same* direction (sign) as velocity

$prop$: Proportional to

A more general equation for kinematics:
$$ arrow(r)_f = arrow(r)_i + arrow(v)_i t + 1/2 arrow(a) t^2 $$

- Note: Horizontal motion does not affect vertical motion.

## 2025-10-03

When you define the axes of motion, they must be perpendicular. However, they don't necessarily need to be horizontal or vertical. It's good to find 90 degree angles in the scenario and base axes off those.

Breaking motion down into components:
$$ arrow(r) = x hat(i) + y hat(j) $$
$$ Delta arrow(r) = arrow(r)_f - arrow(r)_i $$
$$ arrow(v)_"avg" = (Delta arrow(r)) / (Delta t) = (Delta x) / (Delta t) hat(i) + (Delta y) / (Delta t) hat(j) $$

### 3 cases of 2D motion

1. Circular
2. Parabolic
3. Simple harmonic motion

> **Parabolic motion**
> "Freely falling" object near the surface of the Earth.
> - No influences other than gravity