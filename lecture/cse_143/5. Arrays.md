## Array removals

Sometimes running an algorithm in reverse fixes it.
Can perform in-place by simply replacing previous elements.

## Random

```java
import java.util.*;
Random r = new Random();
r.nextInt();
```