- Use `this` notation to avoid redundancy in constructors
- Always declare preconditions in comments
- Either private field access or public-facing methods can be used for internal class methods
- ALWAYS mention important things a client needs to know about the behaviour of a method (e.g. returning `-1` in `indexOf`)
- ALWAYS avoid redundancy (if **any** 2 lines of code are the exact same)