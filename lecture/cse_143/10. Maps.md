```java
Map<String, Integer> count = new TreeMap<>();
while (input.hasNext()) {
	String next = input.next().toLowerCase();
	if (!count.containsKey(next)) {
		count.put(next, 1);
	} else {
		count.put(next, count.get(next) + 1);
	}
}

for (String word : count.keySet()) {
	System.out.println(count.get(word) + "\t" + word);
}
```

With maps, we usually do something different the first time we see a key.

Note: Java does not have the `[]` operator for maps. It also doesn't initialise new keys (`map.get()` will return `null`).

```java
public static Map<String, Set<String>> readFile(Scanner input) {
	Map<String, Set<String>> friends = ;
	while (input.hasNextLine()) {
		if (line.contains("--)) {
			Scanner lineData = new Scanner(line);
			String name1 = lineData.next();
			lineData.next();
			String name2 = lineData.next();
			
			addTo(friends, name1, name2);
			addTo(friends, name2, name1);
		}
	}
	
	return friends;
}

public static void addTo(Map<String, Set<String>> friends, String name1, String name2) {
	if (!friends.containsKey(name1)) {
		friends.put(name1, new TreeSet<>());
	}
	Set<String> theFriends = friends.get(name1);
	friends.put(name1, theFriends);
	theFriends.add(name2);
}
```