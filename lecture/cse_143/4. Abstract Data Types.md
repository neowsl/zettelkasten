Interfaces: List, Set, Map, Stack, Queue

**LIFO**: Last in, first out: Stack
**FIFO**: First in, first out: Queue

- Note: Interfaces restrict method calls (e.g. `get()` cannot be called on a Queue that uses a LinkedList).

```java
import java.util.*;
```

- Note: You can "rotate" through a queue by placing removed objects in the end!

**Auxiliary structure**: An extra object to store data

- Note: Save the size of a structure before you change it.